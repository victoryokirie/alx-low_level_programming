#include "main.h"
#include <stdio.h>
/**
*_puts - function that prints a string and a new line to stdout
*@str: string to be printed to stdout
*Author: VO
*Return: 0 (success)
**/
int _puts(char *str)
{
	puts(str);
	return (0);
}
